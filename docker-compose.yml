version: '3.8'

services:
  app:
    build: .
    ports:
      - "8008:8008"
    env_file:
      - .env
    # All environment variables are loaded from .env file
    # Uses VITAL_ENV prefix pattern (e.g., DEV_TOOL_WEATHER_API_KEY)
